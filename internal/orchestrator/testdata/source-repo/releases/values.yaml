apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kargo
  namespace: kargo
spec:
  values:
    api:
      adminAccount:
        enabled: true
      tls:
        enabled: false
